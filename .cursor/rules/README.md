# Cursor Rules

Per-layer rules apply when working in the corresponding directory (via globs). See `backend.mdc`, `frontend.mdc`, `infrastructure.mdc`, and `codegen.mdc`; they activate for `backend/**`, `frontend/**`, `infrastructure/**`, and `codegen/**` respectively.
