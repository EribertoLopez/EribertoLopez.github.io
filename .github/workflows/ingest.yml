# Run Document Ingestion Pipeline
name: Ingest Documents

on:
  workflow_dispatch:
    inputs:
      force_reingest:
        description: "Force re-ingestion (replaces existing embeddings)"
        type: boolean
        default: false

jobs:
  ingest:
    runs-on: ubuntu-latest
    environment: production
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_INGESTION_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION || 'us-east-1' }}

      - name: Install boto3
        run: pip install boto3

      - name: Debug info
        run: |
          python --version
          python -c "import boto3; print('boto3', boto3.__version__)"
          echo "Memory: $(free -m | head -2)"
          ls -la frontend/content/_resumes/ frontend/content/_posts/ frontend/content/_projects/

      - name: Run ingestion
        run: python scripts/ingest.py
        timeout-minutes: 10
        env:
          BEDROCK_EMBED_MODEL_ID: amazon.titan-embed-text-v2:0
          EMBEDDINGS_S3_BUCKET: ${{ secrets.EMBEDDINGS_S3_BUCKET }}
          EMBEDDINGS_S3_KEY: chat/embeddings.json

      - name: Verify
        run: |
          SIZE=$(aws s3api head-object \
            --bucket "${{ secrets.EMBEDDINGS_S3_BUCKET }}" \
            --key "chat/embeddings.json" \
            --query 'ContentLength' --output text)
          echo "✅ Embeddings uploaded: ${SIZE} bytes"
